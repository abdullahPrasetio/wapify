# Wapbolt Local — appId/nama/executable BEDA dari apps/desktop (§11.4) supaya
# kedua app bisa terinstal berdampingan dengan userData terpisah.
appId: io.wapbolt.local
productName: Wapbolt Local
directories:
  buildResources: build
  output: dist
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json,vitest.config.ts}'
asar: true
asarUnpack:
  - resources/**
  # better-sqlite3 native addon tidak bisa di-load dari dalam asar
  - '**/node_modules/better-sqlite3/**'
compression: maximum
mac:
  identity: null
  target:
    - dmg
    - zip
  icon: build/icon.icns
win:
  target: nsis
  icon: build/icon.ico
  executableName: wapbolt-desktop-local
linux:
  target:
    - AppImage
    - deb
  icon: build/icon.png
  category: Utility
dmg:
  artifactName: ${name}-${version}.${ext}
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  createDesktopShortcut: always
publish:
  provider: github
  owner: abdullahPrasetio
  repo: wapbolt-lite-releases
  # Tidak set releaseType — electron-builder otomatis menandai release
  # sebagai prerelease kalau versi package.json punya prerelease tag semver
  # (mis. "1.0.0-beta.3"), sama seperti perilaku sebelumnya.
npmRebuild: false
